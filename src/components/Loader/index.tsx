import CircularProgress from "@mui/material/CircularProgress";

// const Loader = styled(CircularProgress)(({ theme }) => ({
//   "& svg circle": { stroke: theme.palette.tip.primaryText },
// }));

export default CircularProgress;
